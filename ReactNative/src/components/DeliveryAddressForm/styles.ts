import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  locationButtons: {
    marginBottom: 24,
  },
  textInput: {
    marginBottom: 24,
  },
  checkboxField: {
    marginTop: 20,
    marginBottom: 24,
    alignSelf: 'flex-end',
  },
  checkbox: {
    alignItems: 'center',
  },
});
